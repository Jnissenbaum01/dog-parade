
<div class="card img-fluid bg-info">
  <div class="card-body">
    <h5 class="card-title">Session History</h5>
    <h6 class="card-subtitle mb-2 text-muted">Dog History</h6>
    <ol>
      <% if (dogHistory) { %>
        <% dogHistory.forEach(item=>{ %>
            <li> <%= item.name %> </li>
        <% }) %>
      <% } %> %>
    </ol>
  </div>
</div>
